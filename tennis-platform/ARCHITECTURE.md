# Tennis Platform - System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER DEVICES                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ Desktop  │  │  Mobile  │  │  Tablet  │  │   App    │  │
│  │ Browser  │  │ Browser  │  │ Browser  │  │ (Phase2) │  │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘  │
└────────┼─────────────┼─────────────┼─────────────┼────────┘
         │             │             │             │
         └─────────────┴─────────────┴─────────────┘
                          │
                    HTTPS/REST API
                          │
         ┌────────────────┴────────────────┐
         │                                  │
┌────────▼───────────┐          ┌──────────▼─────────┐
│   FRONTEND         │          │   CDN / STATIC     │
│   (React SPA)      │          │   (Netlify)        │
│                    │          │                    │
│ • Registration     │          │ • Images           │
│ • Profile Pages    │          │ • CSS/JS Assets    │
│ • Match Scheduling │          │ • Cached Content   │
│ • Tournament Views │          │                    │
│ • Messaging UI     │          │                    │
└────────────────────┘          └────────────────────┘
         │
         │ API Calls (JWT Auth)
         │
┌────────▼──────────────────────────────────────────────────┐
│                    BACKEND API SERVER                      │
│                (Node.js + Express)                         │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              API ROUTES                              │ │
│  │  • /api/auth/*     (Registration, Login, Verify)    │ │
│  │  • /api/users/*    (Profiles, Search, Block)        │ │
│  │  • /api/matches/*  (Schedule, Confirm, Score)       │ │
│  │  • /api/courts/*   (Directory, Availability)        │ │
│  │  • /api/tournaments/* (Create, Join, Brackets)      │ │
│  │  • /api/messages/* (Chat, Notifications)            │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              MIDDLEWARE                              │ │
│  │  • JWT Authentication                                │ │
│  │  • Request Validation                                │ │
│  │  • Error Handling                                    │ │
│  │  • Rate Limiting                                     │ │
│  │  • CORS Configuration                                │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              BUSINESS LOGIC                          │ │
│  │  • User Management                                   │ │
│  │  • ELO Rating Calculation                            │ │
│  │  • Match Scheduling                                  │ │
│  │  • Tournament Bracket Generation                     │ │
│  │  • Notification System                               │ │
│  └──────────────────────────────────────────────────────┘ │
└─────────┬──────────────────┬─────────────────┬───────────┘
          │                  │                 │
          │                  │                 │
    ┌─────▼─────┐      ┌─────▼─────┐    ┌─────▼─────┐
    │PostgreSQL │      │  Twilio   │    │AWS S3/    │
    │ Database  │      │    SMS    │    │Cloudinary │
    │           │      │           │    │  Photos   │
    └───────────┘      └───────────┘    └───────────┘
```

---

## 📊 Database Schema Overview

```
┌─────────────────┐
│     USERS       │ ◄────┐
├─────────────────┤      │
│ • id (PK)       │      │
│ • email         │      │ One-to-Many
│ • phone         │      │
│ • password_hash │      │
│ • elo_rating    │      │
│ • profile_data  │      │
└────────┬────────┘      │
         │               │
         │               │
         │    ┌──────────┴──────────┐
         │    │   RATING_HISTORY    │
         │    ├─────────────────────┤
         │    │ • id (PK)           │
         │    │ • user_id (FK)      │
         │    │ • old_rating        │
         │    │ • new_rating        │
         │    │ • match_id (FK)     │
         │    └─────────────────────┘
         │
         │    ┌─────────────────────┐
         ├───►│     MATCHES         │
         │    ├─────────────────────┤
         │    │ • id (PK)           │
         │    │ • player1_id (FK)   │
         │    │ • player2_id (FK)   │
         │    │ • court_id (FK)     │
         │    │ • scheduled_time    │
         │    │ • score             │
         │    │ • winner_id (FK)    │
         │    └──────────┬──────────┘
         │               │
         │               │
         │    ┌──────────▼──────────┐
         ├───►│     COURTS          │
         │    ├─────────────────────┤
         │    │ • id (PK)           │
         │    │ • name              │
         │    │ • address           │
         │    │ • surface           │
         │    │ • price_per_hour    │
         │    └─────────────────────┘
         │
         │    ┌─────────────────────┐
         ├───►│   TOURNAMENTS       │
         │    ├─────────────────────┤
         │    │ • id (PK)           │
         │    │ • name              │
         │    │ • format            │
         │    │ • start_date        │
         │    │ • created_by (FK)   │
         │    └──────────┬──────────┘
         │               │
         │               │
         │    ┌──────────▼──────────┐
         │    │  TOURNAMENT_PARTS   │
         │    ├─────────────────────┤
         │    │ • tournament_id(FK) │
         │    │ • user_id (FK)      │
         │    │ • seed              │
         │    │ • standing          │
         │    └─────────────────────┘
         │
         │    ┌─────────────────────┐
         └───►│   MESSAGES          │
              ├─────────────────────┤
              │ • id (PK)           │
              │ • sender_id (FK)    │
              │ • receiver_id (FK)  │
              │ • content           │
              │ • read              │
              └─────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────┐
│  Client  │
└─────┬────┘
      │
      │ 1. POST /api/auth/register
      │    { email, phone, password, ... }
      │
      ▼
┌─────────────┐
│   Backend   │──── 2. Hash password (bcrypt)
│             │
│             │──── 3. Store in database
│             │
│             │──── 4. Generate verification code
│             │
│             │──── 5. Send SMS via Twilio
└─────┬───────┘
      │
      │ 6. Return JWT token
      │    { token, user }
      │
      ▼
┌──────────┐
│  Client  │──── 7. Store token in localStorage
└─────┬────┘
      │
      │ 8. Include token in future requests
      │    Authorization: Bearer <token>
      │
      ▼
┌─────────────┐
│   Backend   │──── 9. Verify token (JWT middleware)
│             │
│             │──── 10. Attach user to request
│             │
│             │──── 11. Process request
└─────────────┘
```

---

## 🎮 User Registration Flow

```
START
  │
  ▼
┌─────────────────────┐
│ User fills form:    │
│ • Name              │
│ • Email             │
│ • Phone             │
│ • Password          │
│ • Skill Level       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Frontend validates  │
│ • Required fields   │
│ • Email format      │
│ • Password strength │
│ • Phone format      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐      ❌ Error
│ POST /auth/register │────────────► Show error message
└──────────┬──────────┘
           │ ✅
           ▼
┌─────────────────────┐
│ Backend processes:  │
│ • Hash password     │
│ • Create user       │
│ • Generate code     │
│ • Send SMS          │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Return token + user │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Frontend stores:    │
│ • Token → localStorage
│ • User → Zustand    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Show verification   │
│ code input screen   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ User enters code    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐      ❌ Invalid
│ POST /auth/         │────────────► Show error
│      verify-phone   │
└──────────┬──────────┘
           │ ✅
           ▼
┌─────────────────────┐
│ Phone verified!     │
│ Redirect to profile │
└─────────────────────┘
  │
  ▼
END
```

---

## 🔄 ELO Rating Update Flow

```
┌─────────────┐
│ Match Ends  │
└──────┬──────┘
       │
       ▼
┌──────────────────────┐
│ Get both players'    │
│ current ratings:     │
│ • Player A: 1450     │
│ • Player B: 1350     │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Calculate expected   │
│ score using formula: │
│ E = 1/(1+10^(diff/400))
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Determine K-factor:  │
│ • Provisional: K=32  │
│ • Established: K=16  │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Calculate new rating:│
│ New = Old + K(S - E) │
│ Where S = 1 (win)    │
│       or 0 (loss)    │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Update database:     │
│ • users.elo_rating   │
│ • rating_history     │
│ • users.wins/losses  │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Check if provisional:│
│ matches_for_elo >= 10?
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Update leaderboards  │
└──────────────────────┘
```

---

## 📱 Feature Dependencies

```
Feature #1: Player Profiles (✅ COMPLETE)
    ├── Required for all other features
    └── Provides: Authentication, User data

Feature #2: Match Scheduling
    ├── Depends on: Feature #1
    ├── Required for: Feature #6 (Rating updates)
    └── Optional for: Feature #4 (Tournaments)

Feature #3: Court Finder
    ├── Depends on: Feature #1
    ├── Enhances: Feature #2 (Court selection)
    └── Optional for: Feature #4 (Tournament venues)

Feature #4: Tournament Management
    ├── Depends on: Feature #1, Feature #2
    ├── Uses: Feature #3 (venues)
    └── Enhances: Feature #6 (competitive play)

Feature #5: Messaging System
    ├── Depends on: Feature #1
    ├── Enhances: Feature #2, #4
    └── Independent feature

Feature #6: Rating/Ranking System
    ├── Depends on: Feature #1, Feature #2
    ├── Enhances: Feature #4
    └── Core competitive feature
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────┐
│           PRODUCTION ENVIRONMENT             │
└─────────────────────────────────────────────┘

        Users (Global)
              │
              ▼
    ┌─────────────────┐
    │  Cloudflare CDN │ (Optional)
    │  • DDoS Protection
    │  • SSL/TLS
    │  • Caching
    └────────┬────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌──────────┐    ┌──────────┐
│ Netlify  │    │ Railway  │
│ Frontend │    │ Backend  │
│          │    │ API      │
│ • React  │    │ • Node.js│
│ • Static │    │ • Express│
│ • HTTPS  │    │ • HTTPS  │
└──────────┘    └────┬─────┘
                     │
          ┌──────────┼──────────┐
          │          │          │
          ▼          ▼          ▼
    ┌─────────┐ ┌─────────┐ ┌─────────┐
    │Railway  │ │ Twilio  │ │   AWS   │
    │PostgreSQL│ │   SMS   │ │   S3    │
    └─────────┘ └─────────┘ └─────────┘
```

---

## 📊 Data Flow Examples

### Example 1: Player Search

```
1. User enters search criteria
   ↓
2. Frontend validates input
   ↓
3. GET /api/users/search?minRating=1000&maxRating=1500
   ↓
4. Backend auth middleware validates JWT
   ↓
5. Controller queries database with filters
   ↓
6. Database returns matching users (excluding blocked)
   ↓
7. Backend formats response
   ↓
8. Frontend receives JSON
   ↓
9. React renders player cards
   ↓
10. User clicks player → Navigate to profile
```

### Example 2: Match Creation

```
1. User selects opponent
   ↓
2. Fills match details (date, time, court)
   ↓
3. POST /api/matches { opponent_id, scheduled_time, court_id }
   ↓
4. Backend validates:
   - User is authenticated
   - Opponent exists
   - Time is in future
   - Court exists
   ↓
5. Create match record
   ↓
6. Create invitation record
   ↓
7. Send notification to opponent (Feature #5)
   ↓
8. Return match details
   ↓
9. Frontend shows success
   ↓
10. Navigate to match details page
```

---

## 🔒 Security Layers

```
Layer 1: Network
├── HTTPS/TLS encryption
├── Cloudflare DDoS protection
└── Firewall rules

Layer 2: Application
├── JWT authentication
├── Password hashing (bcrypt)
├── Input validation
├── SQL injection protection (parameterized queries)
└── Rate limiting

Layer 3: Data
├── Database access controls
├── Encrypted passwords
├── Private profile settings
└── User blocking mechanism

Layer 4: Monitoring
├── Error logging
├── Suspicious activity alerts
└── Access logs
```

---

## 🎯 API Response Format

### Success Response
```json
{
  "success": true,
  "data": {
    // Response data here
  },
  "message": "Optional success message"
}
```

### Error Response
```json
{
  "success": false,
  "error": "Error message",
  "details": "Optional error details"
}
```

---

This architecture is designed to be:
- ✅ **Scalable**: Can handle growth from 100 to 10,000+ users
- ✅ **Maintainable**: Clear separation of concerns
- ✅ **Secure**: Multiple security layers
- ✅ **Cost-effective**: Uses free/cheap tiers initially
- ✅ **Extendable**: Easy to add new features

Ready to build more features on this foundation!
